CREATE TABLE itsadate_calendars (
    id  INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    calendar_name TEXT NOT NULL,
    user_id INTEGER REFERENCES itsadate_users(id) ON DELETE CASCADE NOT NULL
);